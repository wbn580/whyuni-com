---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import BaseHead from '../components/BaseHead.astro';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  canonicalURL?: string;
  schema?: string;
}

const {
  title,
  description,
  ogImage,
  canonicalURL,
  schema,
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title={title}
      description={description}
      ogImage={ogImage}
      canonicalURL={canonicalURL}
      schema={schema}
    />
    <style is:global>
      @import '../styles/global.css';
    </style>
  </head>
  <body>
    <Header />
    <main><slot /></main>
    <Footer />
    <!-- Per-site widget -->
        <!-- 留资入口中心化组件（吸底两按钮 + 桌面药丸 + 文中图片卡） -->
    <script src="https://chat.whyuni.com/lead.js" data-site="whyuni-com" data-lang="en" data-color="#1D3461" data-accent="#000c39" async></script>
</body>
</html>
