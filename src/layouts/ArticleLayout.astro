---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  description: string;
  publishDate: string;
  category: string;
  readingTime?: number;
  ogImage?: string;
  sources?: Array<{ title: string; url: string; type: string }>;
}

const {
  title,
  description,
  publishDate,
  category,
  readingTime,
  ogImage,
} = Astro.props;

const siteUrl = 'https://whyuni.com';
const slug = Astro.url.pathname;
const articleSchema = JSON.stringify({
  '@context': 'https://schema.org',
  '@type': 'Article',
  headline: title,
  description: description,
  datePublished: publishDate,
  dateModified: publishDate,
  author: {
    '@type': 'Organization',
    name: 'Why This University',
  },
  publisher: {
    '@type': 'Organization',
    name: 'Why This University',
    url: siteUrl,
  },
  articleSection: category,
});
---

<BaseLayout
  title={title}
  description={description}
  ogImage={ogImage}
  canonicalURL={`${siteUrl}${slug}`}
  schema={articleSchema}
>
  <article class="max-w-3xl mx-auto px-6 py-12">
    <header class="mb-10">
      <nav class="text-sm text-aus-muted mb-4">
        <a href="/" class="hover:text-amber-2 no-underline">Home</a> /
        <span> {category}</span> /
        <span class="text-aus-ink">{title}</span>
      </nav>
      <h1 class="font-heading text-3xl md:text-4xl text-aus-ink font-bold leading-tight mb-4">
        {title}
      </h1>
      <div class="flex items-center gap-4 text-sm text-aus-muted">
        <time datetime={publishDate}>{new Date(publishDate).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}</time>
        {readingTime && <span>{readingTime} min read</span>}
      </div>
    </header>

    <div class="prose prose-lg max-w-none">
      <slot />
    </div>

    <footer class="mt-16 pt-8 border-t border-white/10">
      <nav class="flex justify-between text-sm">
        <a href="/" class="text-amber-2 hover:text-amber no-underline">&larr; Back to Home</a>
        <a href="/#notes" class="text-amber-2 hover:text-amber no-underline">
          More decision notes &rarr;
        </a>
      </nav>
    </footer>
  </article>
</BaseLayout>
